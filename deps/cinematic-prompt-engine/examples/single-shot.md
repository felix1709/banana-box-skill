# Single Shot Example

User:

```text
用 Succession 风格，一个女人在雨夜停车场独自走向车，中文 prompt。
```

Expected behavior:

- Exact preset match: `succession`.
- Assemble directly.
- Use the preset's framing, camera, lighting, color, mood, and G1 anti-slop bucket.
- Output one still-image prompt only.
