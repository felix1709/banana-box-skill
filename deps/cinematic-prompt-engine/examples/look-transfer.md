# LOOK Transfer Example

User:

```text
用 dune_arrakis 的 LOOK，拍一座清晨雾中的未来神庙。
```

Expected behavior:

- Load `dune_arrakis` LOOK layers.
- Replace the scene with the future temple.
- Keep lighting, color, texture, mood, and anti-slop bucket from the preset.
- Output one still-image prompt only.
